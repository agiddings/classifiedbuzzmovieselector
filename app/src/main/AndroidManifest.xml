<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="classified.classifiedbuzzmovieselector">

    <uses-sdk
        android:minSdkVersion="11"
        android:targetSdkVersion="21" />

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".controllers.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".controllers.RegistrationActivity"
            android:theme="@style/AppTheme" />
        <activity android:name=".controllers.HomeActivity" />
        <activity android:name=".controllers.ProfileActivity" />
        <activity android:name=".controllers.SearchActivity" />
        <activity android:name=".controllers.UnableLoginActivity" />
        <activity
            android:name=".controllers.MovieInformationActivity"
            android:label="@string/title_activity_movie_information"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".controllers.RecommendationActivity"
            android:label="@string/title_activity_recommendation"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".controllers.AdminActivity"></activity>
        <!--<activity android:name=".controllers.UserActivity"></activity>-->
    </application>

</manifest>
